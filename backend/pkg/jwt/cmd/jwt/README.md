`jwt` command-line tool
=======================

A debugging CLI for signing, verifying, and inspecting JWTs, inherited from
upstream `golang-jwt/jwt`.

**Status:** `main.go` in this fork is currently commented out and does not
build. Kept as reference for the intended interface (`-key`, `-alg`, `-sign`,
`-verify`, `-show` flags); not part of the runtime services.
